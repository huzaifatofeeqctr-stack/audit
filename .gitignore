# Secrets — keep local only, never commit (see note in sf-lib.ps1)
sf-auth.json
spotdraft-auth.json

# Downloaded signed contracts (confidential pricing) — keep local only
*(T-*).pdf
_audit_*.pdf
_dl.bin

# env files (secrets — never commit)
.env
.env.local
railway-worker/.env
